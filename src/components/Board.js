import React from 'react';

const Board = () => {
    return (
        <div>
        </div>
    );
};


export default Board;

//we need a list of boards by title to appear in a box under a board header
// title will also appear in selected board? which isnt a component?
//board header will be in the jsx of app.js 

